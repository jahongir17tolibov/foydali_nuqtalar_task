import 'package:flutter/material.dart';

class HomeStyleModel {
  const HomeStyleModel({
    required this.buttonColor,
    required this.textColor,
  });

  final Color buttonColor;
  final Color textColor;
}
